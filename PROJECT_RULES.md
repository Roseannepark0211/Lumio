# PROJECT_RULES.md

# Lumio Project Rules

本项目为个人长期维护项目。

技术栈：

- Python 3.10+
- PySide6
- SQLAlchemy
- SQLite
- Flask Local API
- yt-dlp
- Browser Extension

目标：

- 长期维护
- 优先稳定
- 保持代码统一
- 提高可维护性
- 不破坏已有架构

---

# 一、开发原则

收到任何需求后：

1. 先理解需求。
2. 分析调用链。
3. 分析影响范围。
4. 优先复用已有代码。
5. 选择最小修改方案。
6. 再开始修改。

不要为了实现一个功能而重写整个模块。

---

# 二、修改原则

修改前必须先确认：

- 谁调用该代码？
- 它调用哪些模块？
- 是否影响 GUI？
- 是否影响数据库？
- 是否影响线程？
- 是否影响下载流程？

如果影响多个模块，请提前说明。

---

# 三、Code Review

收到代码后，不要直接修改。

先检查：

- 是否存在 Bug
- 是否存在重复代码
- 是否存在职责过重
- 是否影响架构
- 是否影响线程
- 是否影响 GUI
- 是否影响数据库
- 是否存在性能问题
- 是否存在字符编码问题

确认后再修改。

---

# 四、项目规则

优先复用已有实现。

禁止：

- 新建 Downloader2
- 新建 QueueManager2
- 新建 Widget2
- 新建 Helper2
- 新建 Utils2
- 复制已有逻辑重新实现

如果已有功能可以扩展，必须扩展原实现。

---

# 五、GUI 规则

GUI 只负责：

- 页面展示
- 用户交互
- Signal

GUI 不负责：

- 下载
- SQL
- 网络请求
- 文件 IO
- 长时间计算

所有耗时操作必须放后台线程。

---

# 六、多线程规则

后台线程：

禁止直接修改 Widget。

GUI 更新统一使用 Signal。

避免：

- 死锁
- 锁嵌套
- 线程泄漏
- 资源泄漏

---

# 七、数据库规则

SQLAlchemy：

- Session 生命周期清晰
- 不跨线程共享 Session
- 避免重复 Query
- 避免重复 Commit

修改数据库逻辑时，考虑是否影响：

- Library
- History
- Inbox

---

# 八、下载模块规则

修改 Downloader、Queue 或下载流程时，必须同时检查：

- Queue
- Retry
- Pause
- Resume
- Cancel
- History
- Library
- Inbox
- Notification

保证状态一致。

不要只修改单个模块。



**平台统一数据模型**

 所有平台（YouTube、Instagram、X、微博、小红书、B站、抖音等）最终必须输出统一的数据结构。

 禁止： 

- 某个平台返回 dict 

- 某个平台返回 tuple

-  某个平台返回 list

-  某个平台直接返回 JSON 

  所有 Provider 必须最终转换为： 

  ​							MediaInfo    

  ​								└── List[MediaItem] 

  GUI、Queue、Downloader、Library 不应感知平台差异，只处理统一的数据模型。

---

# 九、代码风格

保持与项目现有风格一致。

优先：

- 可维护
- 可读
- 可扩展

不要为了"更优雅"而重构整个项目。

新增代码必须像项目原本就存在的一部分。

---

# 十、回答要求

修改代码前：

先分析。

修改代码后：

说明：

- 修改内容
- 修改原因
- 风险
- 影响范围

如果发现当前实现不合理，可以提出更好的方案，但不要擅自大规模重构。

---

# 最终原则

始终遵循：

1. 保持项目架构一致。
2. 优先复用已有实现。
3. 最小化修改。
4. 不破坏已有功能。
5. 不引入无必要的新架构。
6. 保持整个项目风格统一。

如果实现方案与项目现有架构冲突，应优先遵循项目架构，而不是采用理论上的最佳实践。