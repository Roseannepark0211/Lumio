# REFACTOR_GUIDE.md

# Lumio 重构指南

> 本文档用于指导 AI 对 Lumio 项目进行代码审查与重构。
>
> 重构目标是在 **保持功能完全一致** 的前提下，提高代码质量、可维护性、可扩展性和稳定性。
>
> **禁止为了重构而重构。**

---

# 一、重构目标

所有重构必须遵循以下目标：

- 保持现有功能完全一致
- 保持用户操作方式一致
- 保持接口兼容
- 保持项目始终可运行
- 优先提高可维护性
- 优先降低耦合
- 优先减少重复代码

不得为了追求理论上的最佳实践而破坏当前架构。

---

# 二、重构原则

所有修改必须遵循：

1. 最小修改原则
2. 优先复用已有实现
3. 保持项目风格一致
4. 保持接口兼容
5. 不影响已有功能
6. 不新增无意义的新架构

禁止：

- 重写整个模块
- 推翻现有架构
- 修改无关代码
- 为了"更优雅"而进行大规模重构

---

# 三、重构流程

收到代码后，请严格按照以下流程执行。

## 第一阶段：Code Review

不要直接修改代码。

先分析当前文件。

检查：

### 架构

- 模块职责是否清晰
- 是否存在职责过重
- 是否存在高耦合
- 是否存在隐藏依赖

### 代码质量

检查：

- 重复代码
- 方法过长
- 类过大
- 命名不合理
- 可读性差
- 注释不足
- 死代码
- 无用代码

### GUI（PySide6）

检查：

- 页面是否职责过重
- 是否阻塞 GUI
- Signal 是否合理
- Widget 是否可以拆分
- 是否存在重复 UI

### 多线程

检查：

- 是否线程安全
- 是否存在竞争条件
- 是否存在死锁风险
- 是否正确释放线程
- 是否跨线程更新 Widget

### 数据库

检查：

- Session 生命周期
- Query 是否重复
- Commit 是否合理
- 是否跨线程访问数据库

### 性能

检查：

- 重复 IO
- 重复数据库查询
- 重复对象创建
- UI 重绘
- 图片加载
- 缩略图
- JSON 读取

对于每一个问题，请说明：

- 原因
- 风险
- 是否建议修改

不要直接修改代码。

---

## 第二阶段：制定重构方案

完成 Review 后，

输出：

### 当前评分

例如：

90 / 100

---

### 优点

列出当前实现合理的地方。

---

### 存在的问题

列出所有建议修改的问题。

---

### 修改收益

按照优先级排序：

★★★★★ 必须修改

★★★★ 推荐修改

★★★ 有时间再优化

★★ 可以保留

★ 不建议修改

同时说明：

- 修改收益
- 风险
- 是否影响其它模块

---

## 第三阶段：开始重构

一次只重构一个文件。

不要同时修改多个模块。

保持：

- 接口兼容
- 数据兼容
- 功能兼容

不要顺手修改其它代码。

不要引入新的设计模式。

不要新增复杂抽象。

---

## 第四阶段：输出结果

完成修改后，请说明：

### 修改内容

修改了哪些代码。

---

### 修改原因

为什么这样修改。

---

### 风险

是否存在风险。

---

### 影响范围

是否影响：

- GUI
- 数据库
- 下载模块
- Queue
- Library
- History
- Inbox
- Notification

---

### 是否建议继续重构

如果当前文件还有问题，可以继续提出建议。

否则明确说明：

> 当前文件无需继续重构。

---

# 四、重点检查模块

修改以下模块时，请特别注意：

## Downloader

必须检查：

- Retry
- Resume
- Pause
- Cancel
- Queue
- History
- Library
- Notification
- Inbox

保证状态一致。

---

## GUI

必须检查：

- 页面职责
- Signal
- Slot
- 后台线程
- 页面刷新
- Widget 生命周期

---

## 数据库

必须检查：

- Session
- Commit
- Rollback
- Query
- ORM

---

## 多线程

必须检查：

- Signal
- Lock
- QThread
- Worker
- GUI 更新

---

# 五、禁止事项

禁止：

❌ 重写整个模块

❌ 删除已有功能

❌ 修改数据库结构（除非明确要求）

❌ 引入新的框架

❌ 引入新的架构

❌ 修改无关代码

❌ 新建：

- Downloader2
- QueueManager2
- Widget2
- Helper2
- Utils2

如果已有实现可以扩展，

必须扩展已有实现。

---

# 六、最终原则

如果当前实现已经足够合理，

请明确说明：

> 建议保持现状。

不要为了重构而重构。

任何修改都必须符合：

- 最小修改
- 最低风险
- 易维护
- 易扩展

始终保证：

**项目保持稳定、可运行，并与现有代码风格保持一致。**